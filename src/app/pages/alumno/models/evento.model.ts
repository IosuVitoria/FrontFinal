export interface Evento {
  id: number;
  titulo: string;
  descripcion: string;
  start: string; // Agrega la propiedad start de tipo string
  end: string; // Agrega la propiedad end de tipo string
  fecha: string;
  color: string;
}
